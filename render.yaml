services:
  - type: web
    name: vedicyog
    runtime: python-3.11
    buildCommand: pip install --timeout 120 -v -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11
